<h1>Add a Review</h1>

<%= simple_form_for([@restaurant, @review]) do |f| %>
  <%= f.input :rating, collection: [0, 1, 2, 3, 4, 5], prompt: "Select a rating" %>
  <%= f.input :content %>
  <%= f.submit %>
<% end %>
